<% provide :title, "大喜利参加者の皆様" %>
<h1>大喜利参加者の皆様</h1>

<%= will_paginate %>

<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= render user %>
    </li>
  <% end %>
</ul>

<%= will_paginate %>